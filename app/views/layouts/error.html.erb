<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= content_for?(:title) ? yield(:title) : "Error" %></title>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      line-height: 1.6;
      color: #374151;
      background-color: #f9fafb;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1.5rem;
    }

    .error-container {
      text-align: center;
      max-width: 28rem;
      width: 100%;
    }

    .error-icon {
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 1.5rem;
    }

    .error-icon svg {
      width: 4rem;
      height: 4rem;
      color: #0d9488;
    }

    .error-code {
      font-size: 5rem;
      font-weight: 800;
      letter-spacing: -0.025em;
      color: #0d9488;
      line-height: 1;
      margin-bottom: 0.75rem;
    }

    .error-divider {
      width: 4rem;
      height: 0.25rem;
      background-color: #0d9488;
      border: none;
      border-radius: 9999px;
      margin: 0 auto 1.25rem;
    }

    .error-heading {
      font-size: 1.5rem;
      font-weight: 700;
      color: #111827;
      margin-bottom: 0.5rem;
    }

    .error-description {
      font-size: 1rem;
      color: #6b7280;
      margin-bottom: 2rem;
      line-height: 1.625;
    }

    .error-cta {
      display: inline-block;
      padding: 0.625rem 1.75rem;
      background-color: #0d9488;
      color: #fff;
      font-size: 0.9375rem;
      font-weight: 600;
      text-decoration: none;
      border-radius: 0.5rem;
      transition: background-color 0.15s ease;
    }

    .error-cta:hover { background-color: #0f766e; }
    .error-cta:focus { outline: 2px solid #0d9488; outline-offset: 2px; }

    .error-footer {
      margin-top: 3rem;
      font-size: 0.75rem;
      color: #9ca3af;
    }

    .error-footer span { font-weight: 600; }

    /* Responsive: tighten up on small screens */
    @media (max-width: 480px) {
      .error-code { font-size: 3.5rem; }
      .error-heading { font-size: 1.25rem; }
      .error-icon svg { width: 3rem; height: 3rem; }
    }

    /* Dark mode */
    @media (prefers-color-scheme: dark) {
      body { background-color: #111827; color: #d1d5db; }
      .error-heading { color: #f9fafb; }
      .error-description { color: #9ca3af; }
      .error-cta { background-color: #14b8a6; }
      .error-cta:hover { background-color: #0d9488; }
      .error-cta:focus { outline-color: #14b8a6; }
      .error-footer { color: #6b7280; }
    }

    /* Reduced motion */
    @media (prefers-reduced-motion: reduce) {
      .error-cta { transition: none; }
    }
  </style>
</head>
<body>
  <div class="error-container">
    <%= yield %>

    <div class="error-footer">
      Powered by <span>Panda CMS</span>
    </div>
  </div>
</body>
</html>
