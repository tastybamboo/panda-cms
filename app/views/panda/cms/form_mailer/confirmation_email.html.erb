<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type">
    <style>
      body { font-family: system-ui, -apple-system, sans-serif; line-height: 1.6; color: #333; margin: 0; padding: 0; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .content { background: #ffffff; padding: 24px; }
      .message { white-space: pre-wrap; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="content">
        <% if @body_content.present? %>
          <div class="message"><%= simple_format(@body_content) %></div>
        <% else %>
          <p>Thank you for your submission to <%= @form.name %>.</p>
          <p>We have received your information and will be in touch if needed.</p>
        <% end %>
      </div>
    </div>
  </body>
</html>
