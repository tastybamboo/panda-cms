import type { Diagnostic } from "@herb-tools/core";
import type { SyntaxRenderer } from "./syntax-renderer.js";
export declare class InlineDiagnosticRenderer {
    private syntaxRenderer;
    constructor(syntaxRenderer: SyntaxRenderer);
    render(path: string, content: string, diagnostics: Diagnostic[], _contextLines: number, showLineNumbers?: boolean, wrapLines?: boolean, maxWidth?: number, truncateLines?: boolean): string;
}
