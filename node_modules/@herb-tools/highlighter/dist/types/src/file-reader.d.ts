import type { Diagnostic } from "@herb-tools/core";
import type { HighlightOptions, HighlightDiagnosticOptions } from "./highlighter.js";
export declare class FileReader {
    private highlighter;
    constructor(highlighter: any);
    highlightFromPath(filePath: string, options?: HighlightOptions): string;
    highlightDiagnosticFromPath(filePath: string, diagnostic: Diagnostic, options?: HighlightDiagnosticOptions): string;
}
