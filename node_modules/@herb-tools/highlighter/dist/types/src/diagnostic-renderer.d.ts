import type { SyntaxRenderer } from "./syntax-renderer.js";
import type { Diagnostic } from "@herb-tools/core";
export interface DiagnosticRenderOptions {
    contextLines?: number;
    showLineNumbers?: boolean;
    optimizeHighlighting?: boolean;
    wrapLines?: boolean;
    maxWidth?: number;
    truncateLines?: boolean;
}
export declare class DiagnosticRenderer {
    private syntaxRenderer;
    constructor(syntaxRenderer: SyntaxRenderer);
    private highlightBackticks;
    private truncateLineForDiagnostic;
    private extractPortionFromPosition;
    renderSingle(path: string, diagnostic: Diagnostic, content: string, options?: DiagnosticRenderOptions): string;
}
