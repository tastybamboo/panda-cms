import type { SyntaxRenderer } from "./syntax-renderer.js";
export interface FileRenderOptions {
    showLineNumbers?: boolean;
    contextLines?: number;
    focusLine?: number;
}
export declare class FileRenderer {
    private syntaxRenderer;
    constructor(syntaxRenderer: SyntaxRenderer);
    renderWithLineNumbers(path: string, content: string, wrapLines?: boolean, maxWidth?: number, truncateLines?: boolean): string;
    renderWithFocusLine(path: string, content: string, focusLine: number, contextLines: number, showLineNumbers?: boolean, maxWidth?: number, wrapLines?: boolean, truncateLines?: boolean): string;
    renderPlain(content: string, maxWidth?: number, wrapLines?: boolean, truncateLines?: boolean): string;
}
