import type { HerbBackend } from "@herb-tools/core";
import type { ColorScheme } from "./themes.js";
export declare class SyntaxRenderer {
    private colors;
    private isColorEnabled;
    private herb;
    constructor(colors: ColorScheme, herb?: HerbBackend);
    initialize(): Promise<void>;
    get initialized(): boolean;
    highlight(content: string): string;
    private applyColor;
    private highlightRubyCode;
    private highlightTokens;
    private updateState;
    private getContextualColor;
}
