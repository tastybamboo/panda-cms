<div class="" data-controller="dashboard">
  <%= render Panda::Core::Admin::ContainerComponent.new do |container| %>
    <% container.heading(text: "Dashboard", level: 1) do |heading| %>
      <% heading.button(action: :add, text: "Add Page", link: new_admin_cms_page_path) %>
    <% end %>

    <div class="mt-8 p-6 bg-gray-50 dark:bg-gray-800 rounded-lg">
      <h2 class="text-lg font-semibold text-gray-900 dark:text-gray-100 mb-2">Welcome to Panda CMS</h2>
      <p class="text-gray-600 dark:text-gray-400">Navigate to Posts to see the Version History feature.</p>
    </div>
  <% end %>
</div>
